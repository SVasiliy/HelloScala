object MapTest {

  def main (args:Array[String]): Unit = {

    val myMap = Map("MI" → "Michigan", "OH" → "Ohio", "WI" → "Wisconsin", "MI" → "Michigan")

    val mapValues = myMap.values

    println(mapValues) // function

  }

}
